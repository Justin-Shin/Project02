<h2>Recipes</h2>
<ul>
  <% @recipes.each do |recipe| %>
    <li><%= link_to recipe.name, recipe_path(recipe) %> - Requires:
    <% recipe.foods.each do |food| %>
      <%= link_to food.name, food_path(food) %>,
    <% end %>
    etc.
    </li>
  <% end %>
</ul>
